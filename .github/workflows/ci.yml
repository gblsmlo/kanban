name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    uses: gblsmlo/github-workflows/.github/workflows/bun-library-ci.yml@v1.0.0
    with:
      build-storybook: true
